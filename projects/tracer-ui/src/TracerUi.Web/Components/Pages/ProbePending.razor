@*
    What a write surface says while the probe of the installed bd runs. No
    control can say what it does until that probe answers, so the surface says
    why it waits instead of drawing a control that refuses a press.
*@

<p class="text-muted">The app is asking bd what it can do…</p>
