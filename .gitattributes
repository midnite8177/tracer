# Git stores every text file with LF, and checks it out with LF on every
# platform. A file with mixed endings cannot come back.
* text=auto eol=lf

# Vendored third-party code keeps the bytes that its publisher shipped.
projects/tracer-ui/src/TracerUi.Web/wwwroot/lib/** -text

.gitignore -text
